/**
 * Persisted table view state — see app/_components/use-persisted-state.ts for
 * the full rationale. The hook moved there when The Wire adopted the same
 * pattern; this re-export keeps the watchlist's import path stable.
 */

export { usePersistedState } from "@/app/_components/use-persisted-state";
